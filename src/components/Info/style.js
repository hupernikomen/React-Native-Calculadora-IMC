import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({

    info: {
        flex: 1,
    },

    alerta: {
        fontSize: 22,
        fontWeight: 'bold',
        textAlign: 'center'
    },

    resultado: {
        fontSize: 80,
        fontWeight: 'bold',
        color: '#000',
        textAlign: 'center'
    },

    status: {
        marginTop: 30,
        fontSize: 30,
        color: '#000',
        textAlign: 'center'
    }
})

export default styles